[package]
name = "password-manager-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = { version = "0.4", features = ["serde"] }
clap = { version = "2.34" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

